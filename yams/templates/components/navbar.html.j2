<nav class="navbar navbar-default navbar-fixed-top bg-dark-color a_invert_link" role="navigation">
    <div class="container">
        <div class="navbar-header">

        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse-menu">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>

        <a id="brand" class="navbar-brand font-marckscript" href="#">
            {{ site_name|lower }}<p class="font-helvetica title-breadcrumb">{# ::first:n #}</p>
        </a>
        </div><!-- navbar-header -->

        <div class="collapse navbar-collapse light-color" id="collapse-menu">
            <ul class="nav navbar-nav navbar-left">
                {% if navbar_items %}
                    {% for _nav in navbar_items %}
                        <li>
                            <a href="{{ _nav.link|default('/') }}">
                                {% if _nav.glyphicon %}
                                <span class="glyphicon {{ _nav.glyphicon }}" aria-hidden="true"></span>
                                {% endif %}
                                {{ _nav.text|default('') }}
                            </a>
                        </li>
                    {% endfor %}
                {% endif %}
            </ul>
        </div>

    </div>
</nav>